{% extends 'AppBundle::layout.html.twig' %}

{% block content -%}
    <div class="page-header">
        <h1>Document</h1>
    </div>

    <table class="table">
        <thead>
        <tr>
            <th>Attribute</th>
            <th>Value</th>
        </tr>
        </thead>
        <tbody>
        <tr>
            <td>Id</td>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <td>Name</td>
            <td>{{ entity.name }}</td>
        </tr>
        </tbody>
    </table>

    {{ form(delete_form) }}

    <div class="btn-group" role="group" aria-label="...">
        <a role="button" class="btn btn-default" href="{{ path('document') }}">
            List
        </a>
        <a role="button" class="btn btn-default" href="{{ path('document_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </div>

{% endblock %}
